# Variant `obligation-register` — Research Desk

Must/should/may register from policy text — extract only, not compliance judgment.

Paste `AGENT_MESSAGE.md` with packet field `browser_variant: "obligation-register"`.
