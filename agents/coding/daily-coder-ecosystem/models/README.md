# Model Configuration

Concrete providers are mapped in `config/models.json`. Each specialist owns `agents/<role>/model.json`; changing that file swaps the role without editing orchestration code. Run adapter conformance tests before using a new model.
