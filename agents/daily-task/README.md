# Daily task router

Thin **browser router** for lifestyle and mission-control paste families (not a seventh IDE orchestrator).

**Entry points:** [`ROUTER.yaml`](ROUTER.yaml) · [`driver.py`](driver.py) (`python agents/daily-task/driver.py <family> --json` from repo root) · families under [`browser/families/`](browser/families/).
