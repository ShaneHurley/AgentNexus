# Variant `spec-completeness` — Document Reviewer

**Specs, PRDs, RFCs, SOWs** — implementability and testability review.

**Focus:** undefined terms, untestable shalls, missing NFRs, dependencies, TBDs on critical path.

**Paste:** `AGENT_MESSAGE.md` with `browser_variant: spec-completeness`.

**Pair with:** `writing-studio/tech-doc` for authoring; run this variant in a **new chat** before build kickoff.
