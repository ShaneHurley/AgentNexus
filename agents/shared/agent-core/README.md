# agent-core (stub for personal skills slice)

Canonical home for shared role contracts, style profiles, schemas, and deterministic services.

```bash
pip install -e agent-core
python -m agent_core validate-registry
personal-store --help
writing-lint punctuation path/to/file.md
```

Full platform migration of all specialists into agent-core is deferred. This slice only stubs writing/data/personal needs.
