# Shared Role Registry Reference

Each registry entry defines identity, version, primary outcome, callers, schemas, contract path, permissions, forbidden actions, model tier, and lifecycle state.

Lifecycle values: `experimental`, `active`, `deprecated`, `retired`.

Adapters may narrow canonical permissions. Any widening is a validation error.
