# System map: six orchestrators + personal skills + toolkit

```text
User
  ├── IDE engineering/research
  │     └── six UF agents (AGENTS.md)
  │           deep-research | research-messenger | plan-prep
  │           use-master | daily-coder | researcher
  │
  ├── repeatable personal task
  │     └── skill router (agents/shared/skills/personal-catalog.yaml)
  │            ├── study-hints | professor-email
  │            ├── writing-improver (shared)
  │            ├── career-tools (modes)
  │            └── lab-preflight | deadline-triage (conditional)
  │
  ├── browser everyday (soft drafts)
  │     └── Atlas / Prism / Compass
  │
  └── durable truth
        ├── calendar + tasks + notes
        └── %USERPROFILE%\.config\personal-career\
```

Shared toolkit: `datasheet-extractor` → deterministic calc → `structured-data-evaluator` → `visualization-specifier` (ADR 0002). Claim audit tags: VERIFIED | USER_CONFIRMED | UNKNOWN | UNSUPPORTED.

Mutating engineering runs: PolicyGateway via `ide-bridge` under `agents/ide/`.
