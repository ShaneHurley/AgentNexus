# Variant `field-extract` — Research Desk

Named fields only (ex information-condenser); locators + UNKNOWN — no narrative synthesis.

Paste `AGENT_MESSAGE.md` with packet field `browser_variant: "field-extract"`.
