# Research domain

Hosts **Research Forge** (evidence pipeline, JSONL ledger) and **research browser** families (e.g. `research-desk`).

**Entry points:** [`research-forge/`](research-forge/) (engine + pytest) · [`browser/`](browser/) (paste harness) · IDE import: `python agents/ide/scripts/import_rf_agents.py --check`.
