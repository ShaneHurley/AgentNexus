# Variant `pdf-adversarial` — Document Reviewer

**PDF and report-shaped deliverables** where page boundaries, figures, and scan/OCR quality matter.

**Focus:** stale dates, unsupported charts, missing appendices, table/executive-summary mismatches, unreadable pages → `INSUFFICIENT_EVIDENCE` when appropriate.

**Paste:** `AGENT_MESSAGE.md` with `browser_variant: pdf-adversarial`.

**Not for:** neutral “list every fact in this PDF” → `research-desk/assemble-given`.
