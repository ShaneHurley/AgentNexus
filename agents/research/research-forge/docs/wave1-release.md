# Wave 1 release notes

Mock-first vertical slice: clarify → charter → search → read → extract → verify → compose.

- CLI: `research-forge run --wave1 --request fixtures/wave1_e2e_request.json`
- Baseline: `research-forge baseline`
- Live remains fail-closed (`validate --gate wave_1_live`).

Interface version: `1.0.0` (`research_forge.wave1.interfaces`).
