# Variant `tech-doc` — Writing Studio

How-to, reference, or runbook for engineers/operators from supplied specs and tabs.

## When to use
- Documenting behavior, setup, or operations when source material is in the packet.

## When not
- Extract-only inventory → research-desk/assemble-given. Quality verdict → document-reviewer.

Paste `AGENT_MESSAGE.md` with packet field `browser_variant: "tech-doc"`.

For send-critical output: run `document-reviewer` in a **new chat** after this variant.
