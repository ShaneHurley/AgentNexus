# Variant `security-focus` — Code Reviewer

Security-biased adversarial walk: trust boundaries, AuthZ, injection, secrets in diff, unsafe defaults. Findings must remain **locator-backed** — no invented CVEs or “probably vulnerable” without diff evidence.

Use for auth, payment, PII, admin, or externally exposed endpoint changes.

Packet: `browser_variant: "security-focus"`.
