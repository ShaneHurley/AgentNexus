# Variant `tone-match` — Writing Studio

Match writing style from samples you own or are permitted to use.

## When to use
- Rewriting with a known-good tone reference you supply.

## When not
- Impersonating another person without authorization → BLOCKED. Facts-first doc → tech-doc.

Paste `AGENT_MESSAGE.md` with packet field `browser_variant: "tone-match"`.

For send-critical output: run `document-reviewer` in a **new chat** after this variant.
