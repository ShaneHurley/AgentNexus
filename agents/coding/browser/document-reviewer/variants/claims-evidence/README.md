# Variant `claims-evidence` — Document Reviewer

**Evidentiary sufficiency** — every material claim must trace to in-document citation/support or be marked UNKNOWN.

**Focus:** statistics, causal language, footnote integrity, broken references, over-strong modals on weak sources.

**Output includes:** `CLAIM_REGISTER` in `ROLE_RESULT`.

**Paste:** `AGENT_MESSAGE.md` with `browser_variant: claims-evidence`.

**Not for:** open-web verification unless packet explicitly lists tabs as evidence.
