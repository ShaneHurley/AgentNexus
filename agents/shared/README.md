# Shared agent layer

Cross-cutting assets: **skills** (personal + shared on-demand), **schemas**, **agent-core** contracts, **browser** harness (`_shared`, profiles, build scripts), and small **scripts** helpers.

**Entry points:** [`skills/`](skills/) · [`agent-core/`](agent-core/) (`pip install -e ./agents/shared/agent-core`) · [`browser/_shared/`](browser/_shared/) · layout package [`ai_agents_repo/`](ai_agents_repo/) (`pip install -e ./agents/shared/ai_agents_repo`).
