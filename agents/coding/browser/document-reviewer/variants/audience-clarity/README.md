# Variant `audience-clarity` — Document Reviewer

**Reader-fit and action clarity** for the audience named in the packet.

**Focus:** jargon, ambiguous sentences, missing decision ask / next step / owner, tone mismatch (exec vs practitioner).

**Paste:** `AGENT_MESSAGE.md` with `browser_variant: audience-clarity`.

**Not for:** full rewrite — use `writing-studio`; this variant only reviews and lists `REQUIRED_FIX`.
