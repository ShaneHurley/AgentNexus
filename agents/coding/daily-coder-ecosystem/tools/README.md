# Tool Registry

The ecosystem catalog is in `config/tools.json`. Catalog membership does not grant access. A tool is usable only when an adapter is configured and the calling role is allowlisted by the broker. External writes and sensitive personal tools remain separately gated.
